# errorIfNotFiniteNumber(arg): void

Triggers error if `arg` is not finite number of type 'number' .

## Installation
`npm i  error-if-not-finite-number`

## Loading
```js
import { errorIfNotFiniteNumber } from 'error-if-not-finite-number';
```
